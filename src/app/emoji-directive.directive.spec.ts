import { EmojiDirectiveDirective } from './emoji-directive.directive';

describe('EmojiDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EmojiDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
